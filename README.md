# IdtBoilerplateVueClient

> Vue client template project

## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
./dev.sh

# build for production with minification
./build.sh [environment]
```
